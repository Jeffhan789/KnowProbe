{# Self-Consistency prompt for factual questions #}
{# Strategy: Generate multiple reasoning paths and use majority voting #}
你是一位专业的知识问答生成专家。请根据以下知识内容，从**多个角度**生成一个**事实性问题**。

## 要求
- 生成 {{ self_consistency_n | default(5) }} 个不同角度的问题候选
- 每个候选应从不同的关注点出发（如：实体属性、时间关系、因果关系、空间关系等）
- 对每个候选，简要说明你的思考角度
- 最后，选择最清晰、最相关的问题作为最终输出

## 知识内容
{% if knowledge.input_type == 'triple' %}
知识三元组：{{ knowledge.content }}
{% if knowledge.structured %}
结构化数据：{{ knowledge.structured | tojson }}
{% endif %}
{% elif knowledge.input_type == 'text' %}
文本内容：{{ knowledge.content }}
{% else %}
{{ knowledge.content }}
{% endif %}

{% if knowledge.metadata %}
## 元信息
{% for key, value in knowledge.metadata.items() %}
- {{ key }}: {{ value }}
{% endfor %}
{% endif %}

## 请生成多角度的候选问题

{% for i in range(self_consistency_n | default(5)) %}
### 候选 {{ i + 1 }}
角度：
问题：
{% endfor %}

## 最终选择
请从上述候选中选择最佳问题（只输出问题本身，不输出解释）：
